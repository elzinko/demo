version: '2'
services:
  web:
    image: dockercloud/hello-world
    ports:
      - 8080

  redis:
    image: redis
    links:
      - web

  lb:
    image: dockercloud/haproxy
    ports:
      - 80:80
    links:
      - web
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
